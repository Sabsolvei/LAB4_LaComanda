import { LlamandoMozoPipe } from './llamando-mozo.pipe';

describe('LlamandoMozoPipe', () => {
  it('create an instance', () => {
    const pipe = new LlamandoMozoPipe();
    expect(pipe).toBeTruthy();
  });
});

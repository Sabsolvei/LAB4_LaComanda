import { ElevateSelectedDirective } from './elevate-selected.directive';

describe('ElevateSelectedDirective', () => {
  it('should create an instance', () => {
    const directive = new ElevateSelectedDirective();
    expect(directive).toBeTruthy();
  });
});
